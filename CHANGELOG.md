# 변경 기록

시스템 구성 상세계획 §8 의 관리 원칙:
> **의사결정 기록** — 계산식·밸런스 변경 사유를 CHANGELOG에 남긴다
> (수치를 왜 바꿨는지 잊는 것이 1인 개발 최대 함정)

그래서 이 파일은 "무엇을 바꿨나"보다 **"왜 그 수치여야 했나"**를 적는다.
밸런스 수치는 전부 `src/core/formulas.ts` 의 `B` 테이블과 `src/data/*.json` 에 있다.

---

## 0.2.0 — 실제 지리 76 거점 · 인물 310명 (데이터 단계)

지도와 인물을 통째로 갈아 끼웠다. 게임 규칙(턴 엔진·헥스 전투·AI·외교·이벤트)은 건드리지 않았다.

### 왜 생성기를 두었는가

새 지도(`src/data/mapdata.json`)는 실제 경위도를 투영한 **지리 데이터**다.
좌표·해안선·산맥·길은 다 있지만 게임 수치는 하나도 없다 — 개발치·지형 분류·거점 특성·
인접 그래프가 그것이다. 그 빈칸을 손으로 76번 채우는 대신 `scripts/build-castles.ts` 의
규칙으로 파생시켰다. 규칙이 코드로 남아 있어야 지도를 다시 그렸을 때 같은 판이 재현된다.
인물도 같은 이유로 `scripts/build-officers.ts` 를 거친다. 생성 결과는 커밋한다.

### 인물 명부에 창(window)을 두 벌 둔 이유

300명 명부는 `histAppear`/`histRetire`(실제 활동 연도)와 `age`/`lifespan`(압축용 나이)을
함께 갖고 있다. 처음에는 역사 연도만 쓰려 했으나, 그렇게 거르면 **642년에 살아 있는 인물이
전체 17명**(고구려 3명)이었다. 700년에 235명을 흩뿌린 결과다. 76 거점을 채울 수 없다.

그래서 시나리오가 `roster` 로 어느 창을 쓸지 고르게 했다.

- `historical` — 642·551년. 46명 명부의 손으로 잡은 활동 연도를 우선한다
  (그 값들은 두 시나리오가 성립하도록 고른 것이라 명부의 좁은 경력 연도보다 맞다).
- `compressed` — 원년. 나이를 연도로 되돌려 광개토대왕과 김유신을 같은 판에 세운다.
  246명이 동시에 활동한다.

같은 사람이 두 명부에 다른 id 로 있던 것 15건(`kim_yusin`↔`kimyusin` 등)은
300명 쪽 id 로 모았다. 이벤트·시나리오의 참조도 함께 고쳤다.

### 특기 어휘를 데이터 쪽에서 맞춘 이유

새 명부는 특기를 70종 쓴다. 뜻이 같은 것(`cavalry`↔`cav`, `navy`↔`naval`,
`siege`↔`siegecraft`, `last_stand`↔`resolve` 등 35쌍)을 **기존 id 쪽으로 모았다.**
반대 방향으로 바꾸면 `hasSkill(def, 'cav')` 같은 문자열 호출이 코드 20여 곳에 흩어져 있어
전부 따라 고쳐야 한다. 데이터를 고치는 편이 싸고 되돌리기 쉽다.

겹치지 않는 25종은 새로 등록했고, 그중 계산에 실제로 반영한 것은 네 개다 —
`valor`(근접 +12%) `spear`(장창 +15%) `governance`(개발 +30%) `ambush`(숲·산 +15%).
나머지는 인물 카드에만 나온다. 한 번에 25개를 구현하면 밸런스가 어디서 틀어졌는지
알 수 없게 된다.

### 산성 방어 배율을 40개에서 15개로 줄인 이유

새 지도의 거점 76개 중 40개가 산성(`fort`)이다. 등급만 보고 방어 배율(1.3)을 주면
절반이 넘는 거점을 AI 가 함락시키지 못한다 — 16 거점 판에서 이미 겪은 문제다.
그래서 **사서가 농성으로 기억하는 15곳**(안시성·삼년산성·대야성·매소성·주류성 등)으로
한정했다. 76 중 20%로, 16 거점 판의 비율(31%)보다 오히려 낮다.

### 그 밖에

- `mountain_fortress` → `fort` 로 이름 통일. 지도 데이터의 등급 이름에 맞췄다.
- `CastleDef.routes` 를 `{land, sea}` 로 나눴다. 수군 통행 판정에 필요하다
  (규칙 자체는 다음 단계).
- **가야를 플레이 가능 세력으로** 올렸다. 새 지도에 가야 거점이 8개 있고
  기획서 §2.1 이 "도전 난이도"로 규정한다. 도성 등급 거점이 없어
  금관성을 도성으로 승격했다 — 철과 바다를 함께 쥔 자리다.
- 배치가 명시되지 않은 인물을 **세력 영토에 흩는다.** 전부 도성에 몰면
  압축 캠페인에서 한 성에 60명이 서고 나머지 20성이 텅 빈다.
- 시나리오가 `events` 를 명시하면 그 목록만 발동한다(이미 스키마에 있었으나
  코드가 무시하고 있었다). 원년 캠페인은 서기 연표를 쓰지 않으므로 빈 배열로 둔다.
- 승려·장인 중 3급을 **재야**로 돌렸다. 새 명부는 전원이 소속이 있어
  「탐색」 명령이 할 일이 없었다.
- `npm run simulate` 에 **"시작 → 평균 최종 거점"** 표를 추가했다.
  판이 실제로 움직였는지를 승률보다 먼저 봐야 한다.

### 알려진 상태 — 밸런스는 아직 맞지 않는다

76 거점 판의 승률은 고구려 55.0 / 백제 0.0 / 신라 45.0, **결착률 0%**.
40년을 돌려도 거점이 평균 두세 개밖에 오가지 않는다. 16 거점에 맞춰 둔 수치가
5배 넓은 판에서 성립하지 않는 것이며, 예상한 결과다. 수치 재조정은 별도 단계로 잡았다.
원인 분석은 [백로그](docs/backlog.md) §0 참조.

---

## 0.1.0 — M1 프로토타입 (Phase 0 ~ 3 완료)

### 밸런스 결정과 근거

측정은 전부 `npm run simulate`(AI 자동 대전)와 `npm test`(전투 20회 반복)로 했다.

#### 경제 — `grainPerAgri` 12 → 20, `grainPerTroop` 0.06 → 0.05

첫 통합 실행에서 세 세력 모두 매 턴 "창고가 비어 군사를 잃었다"를 반복했다.
계산해 보니 개발도 70짜리 거점 6개의 연간 곡물 수입이 약 17,700인데,
AI 가 병영 한계까지 채운 79,000명의 연간 병량은 21,300이었다 — 구조적 적자.
수입을 올리고 소모를 낮춰 흑자로 돌렸고, 병영 수용 한계가 자연스러운 병력 상한이 되게 했다.

함께 넣은 제동 두 가지:
- **AI 징병 유보** (`canFeedMore`) — 창고에 3턴치 병량이 없으면 더 뽑지 않는다.
- **보급선** (`resupply`) — 부대는 아군 거점에 닿아 있을 때만 창고에서 병량을 받는다.
  끊기면 굶는다. 기획서 §6.2 의 "보급선 차단 시 사기 붕괴"를 이 자리에서 구현했다.

#### 병종 상성 — 장창보병 대 개마무사

기획서 §6.1 은 장창보병을 "대기병 방어"로 못 박았지만, 첫 수치(상성 1.6, 방어 42)로는
동수 대결에서 장창의 승률이 9/20 에 그쳤다. 개마무사의 원 능력치(공56/방44)가
장창(공28/방42)보다 워낙 높아 상성 배율이 그 격차를 뒤집지 못한 것이다.

→ 장창 `counters.cavalry` 1.6 → **2.0**, 방어 42 → **45**,
  개마무사 `counters.spear` 0.75 → **0.62**, 일반 기병 0.7 → **0.65**.
이제 16/20 으로 장창이 막아 세운다. "개마무사의 돌격을 막아 세우는 유일한 답"이라는
설명문이 실제로 참이 되었다.

#### 산성 농성

`mountainFortressBonus` 1.3 × `wallDefenseBonus` 1.5 조합으로,
같은 병력의 강공은 산성을 20회 중 20회 막아 낸다. 반면 4배 병력 + 공성병기면 함락된다.
기획서 §6.3 의 "농성 지구전 중심"이 수치로 성립한다.

다만 이 때문에 AI 가 성을 전혀 못 치는 문제가 생겨, **포위(병량전)** 를 실질적인
두 번째 선택지로 만들었다 (`aiMinSiegeRatio` 0.55). 강공이 무리면 둘러싸고 말린다.
반대로 포위군이 수비군보다 한참 약하면 성문이 열리고 기습을 당한다 —
약한 병력으로 무한정 성을 봉쇄하고 있는 그림을 막기 위해서다.

#### 승리 조건 — 기본값을 패권으로

통일(전 거점 점령)만 판정하던 때 결착률은 **0%** 였다. 40년을 돌려도 평균 최대 점유가
16거점 중 9.6 에 머물렀다. 마지막 남은 산성 몇 개가 너무 단단했기 때문인데,
그건 위에서 의도한 결과이므로 전투를 약화시키는 대신 기획서 §3 의 두 번째 승리 조건
**패권(타국을 조공국으로 복속)** 을 구현했다.

- 복속 요구는 **청이 아니라 위협**이다. 처음엔 일반 외교와 같은 판정식을 썼는데,
  전쟁 중이라 우호도가 −100 이면 절대 성공할 수 없어 도달 불가능한 조건이었다.
  힘의 격차(`(내 거점/상대 거점 − 1) × 22`, 최대 60)를 주된 항으로 다시 세웠다.
- 조공국은 종주국보다 강해지기 전에는 반기를 들 수 없다. 그러지 않으면 8턴 정전이 끝나는 대로
  독립을 선언해 판이 다시 늘어졌다.

결과: 결착률 0% → **26%**, 평균 결착 75.5턴(약 19년).

#### AI 수정 — 결과가 나빠져 되돌린 것 포함

1. **출진을 내정보다 먼저 결정** — 내정이 먼저 돌면 모든 장수가 명령을 써 버려
   군을 이끌 사람이 남지 않았다. 24턴을 돌려도 부대가 단 하나도 생기지 않던 원인.
2. **공격력과 방어력을 같은 척도로 비교** — AI 가 "병력 수"와 "방어력 점수"를 직접 비교하고
   있어서, 성벽 보정이 붙은 방어력 앞에서는 어떤 병력도 문턱을 넘지 못했다.
   `compositionPower()` 를 만들어 `castleDefensePower()` 와 같은 단위로 재게 했다.
3. **위협 판정 완화** — "국경을 맞댄 적이 나보다 강하다"를 위협으로 치니 전선의 모든 성이
   늘 위협 상태가 되어 AI 가 영원히 수비만 했다. 실제로 적 부대가 다가와 있을 때만 위협으로 본다.
4. **수군 과대평가 교정** — AI 의 병종 선택이 지형을 무시해서, 백제가 육상 전투에 불리한
   수군(평지 계수 0.85, 산악 0.5)을 계속 뽑고 있었다. 평지·구릉·숲 계수의 평균을 곱해
   평가하도록 고쳤다. 백제 승률 10% → 27.5%.
5. **되돌린 것** — "한 전선이라도 벅차면 화평을 청한다"는 규칙을 넣어 보았으나,
   약한 쪽이 웅크리는 동안 강한 쪽이 무저항으로 커져 판이 더 기울었다
   (642년 백제 22%→14%, 551년 고구려 52%→82%). 코드에 주석으로 남기고 제거했다.

#### 시나리오

- **642년** — 백제가 거점 4개로 가장 적은 대신 이 시점 최강의 야전군을 갖도록
  병력·재화를 올렸다(사비 15,000 등). 최종 44.0 / 22.0 / 34.0.
- **551년** — 처음엔 고구려가 8거점을 쥔 채로 시작해 승률 90% 였다. 두 가지를 고쳤다.
  - 기획서 §8 의 시나리오 3 설명("나제동맹의 한강 수복 → 배신 분기")에 맞춰
    **동맹이 한강을 이미 되찾은 시점**으로 판을 다시 짰다. 고구려 6 / 백제 4(한강 하류 포함) / 신라 6.
  - 시나리오별 세력 사정(`factionMods`)을 스키마에 추가해, 551년 고구려의 내분과
    북방 압박을 귀족 지지도 40·공격성 0.55 로 표현했다.
  - 「한강의 배신」 이벤트가 한성을 통째로 넘겨주던 것을 주둔군 −4,000 · 민심 −20 으로 바꿨다.
    공짜 성은 백제를 매번 무력화시켰다. 배신은 전쟁의 시작이지 전쟁의 결과가 아니다.

  최종 51.7 / 3.3 / 45.0. **백제는 여전히 목표 구간 밖이며 미해결이다** — 백로그 참조.

#### 인물 충성도

야심형·용병형 인물의 충성도 감소가 가팔라, 귀족 지지도가 낮은 고구려에서
20턴 만에 연개소문 본인이 이탈하는 일이 벌어졌다. 독재로 지지도를 깎는 당사자가
그 때문에 떠나는 것은 앞뒤가 맞지 않는다. 감소폭을 −1.5 → −0.8 로 낮추고,
독재(autocrat) 특기 보유자는 오히려 자리가 굳도록 +1 로 두었다.

### 그 밖에 고친 것

- 목적지에 도착한 부대가 해산되지 않고 빈 성 앞에 영원히 서서 병량만 축내던 문제
- 야전 공수 판정에서 늘 같은 쪽을 공격 측으로 고르던 버그 (성 주인을 수비로 보게 수정)
- 농성 중인 수비 부대가 성벽을 버리고 평지로 돌격하던 전투 AI (성벽선 안쪽을 지키게 수정)
- 성벽·성문 색이 산악 지형과 거의 같아 화면에서 보이지 않던 문제
- 수비 측으로 전투를 시작하면 적 차례에서 진행이 멈추던 문제 (AI 차례 자동 진행)

### GitHub Pages 배포에서 걸린 함정 (저장소 설정, 코드 아님)

빈 저장소를 공개로 전환하고 `main` 을 처음 만들어 Pages 를 연결하는 과정에서
`pages.yml` 자체는 처음부터 문제없이 돌았는데도 두 곳에서 걸렸다. 둘 다
GitHub 저장소 설정 쪽이라 워크플로 코드로는 못 막는다 — 그래도 적어 둔다.

1. **Pages 소스는 최초 1회 수동 지정이 필요하다.** `actions/configure-pages` 가
   `Get Pages site failed`로 실패했다. 저장소가 한 번도 Pages 를 쓴 적이 없으면
   Settings → Pages → Build and deployment → Source 를 "GitHub Actions" 로
   사람이 먼저 선택해 둬야, 그다음부터 워크플로가 자동으로 배포한다.
2. **`github-pages` 배포 환경은 기본 브랜치에서만 배포를 허용한다.** 1번을 고친 뒤
   `main` 에 푸시해도 `deploy` 잡이 로그 한 줄 없이 즉시 실패했다 — 저장소의
   기본 브랜치가 아직 `main` 이 아니라 예전 작업 브랜치였기 때문. 기본 브랜치를
   `main` 으로 바꾸고 나서야 통과했다.

### 알려진 상태

- 551년 시나리오의 백제 승률 3.3% (목표 25~40%)
- AI 대전 결착률 26% — 나머지는 160턴 시간 초과. 사람이 플레이할 때는 문제가 덜하지만
  AI 끼리는 서로를 끝내지 못하는 경우가 많다.
